# Manifest model emitted by the Computer Unified Memory dispatcher.

js: export const MANIFEST_VERSION = "0.1.0";
js: export const MANIFEST_FILE_NAME = "plan.json";

```go output
package manifest

type Output struct {
    Lane string
    Path string
}
```

```rust plan
pub struct Plan {
    pub language: &'static str,
    pub version: &'static str,
    pub outputs: usize,
}
```

```zig invariants
const std = @import("std");

pub fn writesPlanJson() bool {
    return true;
}
```
